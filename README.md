CREATE TABLE `mahasiswa` (
  `siswa` int NOT NULL,
  `dosen` int NOT NULL,
  `matakulia` int NOT NULL,
  `tabel_umur` int NOT NULL,
  `jumlah_mahasiswa` int NOT NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb3;
COMMIT;
